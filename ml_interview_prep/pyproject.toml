[project]
name = "ml-interview-prep"
version = "0.1.0"
description = "Waymo ML coding prep — drill checker"
requires-python = ">=3.11"
dependencies = [
    "numpy",
    "scipy",
    "scikit-learn",
]

[tool.uv]
package = false
